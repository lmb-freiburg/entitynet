Source is https://github.com/mlfoundations/open_clip

Code is up-to-date with open_clip as of 2025-08 release v3.1.0 with the notable exception of `factory.py` which is currently in a heavily modified older version.

```bash
factory.py  # diverged
model.py  # almost the same. improvements to resize_text_pos_embed, and others
transform.py  # added antialias parameter
```
